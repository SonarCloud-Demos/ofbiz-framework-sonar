# Bootstrap

This root models the private Azure Storage backend used by environment state. Backend coordinates, subscription, tenant, storage name, owners, and cost allocation are external inputs. It is intentionally separate from workload state and remains unapplied under the Phase 2 waiver.
