# Architecture decision records

ADRs record durable modernization decisions. Status values are `Proposed`, `Accepted`, `Deprecated`, or `Superseded`.

An ADR becomes `Accepted` only when its decision authority is named and records approval. Material changes require a new ADR that supersedes the old one; do not rewrite accepted history.
