# Legacy adapters

Anti-corruption adapters may depend on OFBiz implementation code and expose versioned contracts to modern services. Modern services must not depend directly on `applications/`, `framework/`, or `themes/`.
