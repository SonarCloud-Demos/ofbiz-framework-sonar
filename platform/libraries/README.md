# Modern platform libraries

Only thin, domain-neutral security, telemetry, correlation, and error-envelope helpers belong here. Domain models and orchestration are forbidden. Phase 1 services currently use Java platform APIs directly; shared code is introduced only after a second concrete use proves the abstraction.
